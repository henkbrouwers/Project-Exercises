#include<iostream>
#include<cmath>

using std::cout;
using std::cin;

double power(double base, int exponent)
{
    return 0.0;
}

int main()
{
    int base, exponent;
    cout << "What is the base?: ";
    cin >> base;
    cout << "What is the component?: ";
    cin >> exponent;
    double mypower = power(base, exponent)
    // double power = pow(base, component);
    cout << power;
}
